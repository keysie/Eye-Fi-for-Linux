#!/bin/bash

# Small script to handle starting DeepSkyStackerLive.exe with
# wine so the user does not have to cd to its location every
# time.

wine ~/.wine/drive_c/Program\ Files\ \(x86\)/DeepSkyStacker/DeepSkyStackerLive.exe & 

